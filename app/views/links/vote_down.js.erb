console.log("vote_down is running");
$(document).ready(function() {
	console.log("vote_down is ready");
	$('#link_<%= @link.id %> a.down_arrow').toggleClass('selected');
	$('#link_<%= @link.id %> a.up_arrow.selected').toggleClass('selected');
	$('#link_<%= @link.id %> .link_votes').html('<%= "#{@link.plusminus} votes." %>');
});
