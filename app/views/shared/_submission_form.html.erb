<%= form_for @link do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
    
		<p><%= f.label :description %><br />
    <%= f.text_field :description %></p>

		<p><%= f.label :url %><br />
    <%= f.text_field :url %></p>

    <p><%= f.submit "Submit" %></p>
<% end %>